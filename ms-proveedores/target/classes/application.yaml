server:
  port: 8089
  address: 0.0.0.0

spring:
  application:
    name: ms-provedores
  profiles:
    active: dev


  # CONEXIÓN  CON LARAGON (MYSQL)

  #datasource:
   # url: jdbc:mysql://localhost:3306/db_ms_proveedores
    #username: root
    #password: ""
    #driver-class-name: com.mysql.cj.jdbc.Driver


  datasource:
    url: jdbc:oracle:thin:@mmg8hvewk688yrh4_low?TNS_ADMIN=C:/Users/Usuario/Desktop/prueba3FullStrack/ms-proveedores/src/main/resources/Wallet
    username: SCHEMA_PROVEEDORES
    password: FullStack-Bodega1
    driver-class-name: oracle.jdbc.OracleDriver

  # Configuración de JPA e Hibernate
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

# Configuración limpia de Swagger para Spring Boot 3.x
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: alpha

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/    